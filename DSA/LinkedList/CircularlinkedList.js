import { Node } from "../Node";

export default class CircularLinkedList {
  constructor() {
    this.head = null;
    this.size = 0;
  }
  getAtIndex(index) {}
  push(element) {}
  removeAt(index) {}
  insert(element, index) {}
  print() {}
}